{
  "_comment": "Extends base.json, NOT node.json. The engine deliberately does not declare `types: [node]` — it must stay runtime-agnostic so it can be consumed outside Node. Do not 'fix' this to match ts/query.",
  "extends": "@sqleasy/tsconfig/base.json",
  "compilerOptions": {
    "outDir": "./dist"
  }
}
