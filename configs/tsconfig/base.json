{
  "$schema": "https://json.schemastore.org/tsconfig",
  "_comment": "Carried over VERBATIM from the pre-migration sqleasy tsconfig.json, minus `types` — see node.json. Changing anything here changes emitted output for a published package, so treat it as load-bearing.",
  "compilerOptions": {
    "target": "ESNext",
    "lib": ["ESNext"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "declaration": true,
    "sourceMap": true,
    "outDir": "./dist",
    "forceConsistentCasingInFileNames": true,
    "strict": true,
    "skipLibCheck": true
  }
}
